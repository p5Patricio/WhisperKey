# WhisperKey Agent Configuration
# Refer to root AGENTS.md for full project specifications and guidelines.
# Rules:
# 1. Web tooling: ONLY use PNPM (`pnpm`). Never use npm or yarn.
# 2. Conventional commits only. Never add Co-Authored-By or AI attribution.
# 3. Always run `pytest tests/` before finishing tasks to keep all tests green.
# 4. Autoskills command: `npx autoskills`. Skills stored in `.agents/skills/`.
